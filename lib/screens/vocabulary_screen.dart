import 'package:flutter/material.dart';

class VocabularyScreen extends StatelessWidget {
  const VocabularyScreen();

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Vocabulary'));
  }
}
